- urlsnap: SSL support
- Append salts to hidden service addresses before encryption, it's easy to brute-force right now.
- Patch Zathura for a zero-width gutter.
- Birkhoff should synchronise Mnemosyne databases.
- Implement a mesh topology of hosts with spiped.
- On portable devices, a systemd service to spoof MAC addresses on all interfaces at boot.
- Reconstruct ZNC configuration with Nix expressions.
- Rewrite physical specifications to be enough to bring us from a fresh install to something we can deploy to.
- The 'graphical' role describes packages that don't depend on a graphical environment, restructure.
